# Pinned to the exact versions in the .venv that produced the committed notebook
# outputs. Read straight off site-packages, not from memory and not from a
# different machine.
#
# I got this wrong once already, in a way worth recording. I rewrote this file
# against versions I had checked somewhere else, which quietly pinned five packages
# *behind* the environment that generated every number in the report. Installing it
# would have downgraded pandas, numpy, scikit-learn, scipy and matplotlib, and the
# notebooks would have re-run to completion and produced slightly different results
# with no error anywhere. A requirements file that does not match the environment
# behind the results is not reproducibility. It is a decoration that lies.
#
# Python 3.13. Managed with uv.
#   uv venv && uv pip install -r requirements.txt

# Core
pandas==3.0.3
numpy==2.4.6
scipy==1.18.0
joblib==1.5.3

# Modeling
scikit-learn==1.9.0
xgboost==3.3.0

# Explainability and fairness
shap==0.52.0
fairlearn==0.14.0

# Viz
matplotlib==3.11.0

# Notebook, plus headless execution for run_notebooks.sh
jupyterlab==4.6.1
ipykernel==7.3.0
nbconvert==7.17.1
